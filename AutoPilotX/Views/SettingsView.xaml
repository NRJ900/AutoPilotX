<UserControl x:Class="AutoPilotX.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AutoPilotX.Views"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:validation="clr-namespace:AutoPilotX.Validation"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <mah:ToggleSwitch Header="Start with Windows" IsOn="{Binding Settings.StartWithWindows}" />
        <mah:ToggleSwitch Header="Dark Mode" IsOn="{Binding IsDarkMode}" Margin="0,10,0,0" />
        <TextBox Margin="0,10,0,0}">
            <TextBox.Text>
                <Binding Path="Settings.StopHotKey" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <validation:HotkeyValidationRule />
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>
        <Button Content="Save" Command="{Binding SaveSettingsCommand}" Margin="0,10,0,0}" />
    </StackPanel>
</UserControl>
